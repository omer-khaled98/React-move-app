// import { GET_MOVIES } from "../Actions/MoviesAction";

// const INITIAL_STATE = {
//   list: [], // Initial state for storing movies
// };

// const MoviesReducer = (state = INITIAL_STATE, action) => {
//   switch (action.type) {
//     case GET_MOVIES:
//       return {
//         ...state,
//         list: action.payload, // Update the list with the movies data from the action payload
//       };
//     default:
//       return state; // Return the current state if no relevant action is dispatched
//   }
// };

// export default MoviesReducer;
